spring:
  application:
    name: imageliteapi

  datasource:
    url: jdbc:postgresql://localhost:5432/imagelite
    username: seplag
    password: seplag123

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true